# Workshop UI Demo

Next.js + Tailwind 的创意工坊前端 UI（mock 数据）。

## Run

```bash
npm install
npm run dev
```

然后访问：
- `/` 首页
- `/browse` 浏览/搜索/筛选
- `/resource/r_1001` 资源详情 + 评论
- `/upload` 上传（mock）
- `/moderation` 审核队列（mock）
- `/auth/login` 登录（mock）
- `/auth/register` 注册（mock）
